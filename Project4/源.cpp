//#include<stdio.h>//#include<stdlib.h>//#include<math.h>//#include<string.h>//#include<time.h>//#define _CRT_SECURE_NO_DEPRECATE//#pragma warning(disable:4996)//#define N 10//int TestEquation();//随机生成任一矩阵，输出其转置矩阵//void MMul(int(*matrixA)[N], int(*matrixB)[N], int(*matrixC)[N]);//矩阵乘法函数 //void Madd(int(*matrixA)[N], int(*matrixB)[N], int(*matrixC)[N]);//矩阵加法函数 //void show(int(*matrixA)[N]);//输出矩阵内容//////void Madd(int(*matrixA)[N], int(*matrixB)[N], int(*matrixC)[N])//{//	for (int i = 0; i < N; i++)//	{//		for (int j = 0; j < N; j++)//			matrixC[i][j] += (matrixA[i][j] + matrixB[i][j]);//	}//}//void show(int(*matrixA)[N])//{//	for (int i = 0; i < N; i++)//	{//		for (int j = 0; j < N; j++) printf("%-2d  ", matrixA[i][j]);//		printf("\n");//	}////}//void MMul(int(*matrixA)[N], int(*matrixB)[N], int(*matrixC)[N])//矩阵相乘函数 //{//	for (int i = 0; i < N; i++)//	{//		for (int j = 0; j < N; j++)//		{//			for (int k = 0; k < N; k++)//				matrixC[i][j] += matrixA[i][k] * matrixB[k][j];//		}//	}//////}//int TestEquation()//随机生成任一矩阵，输出其转置矩阵//{//	int matrixA[N][N];//	int matrixB[N][N];//	int matrixC[N][N] = { 0 };//	//1 2 N N 2 2 2 1 2//	int m = N, n = N;//	m = rand() % 3 + 3;//3~5阶之间//	n = m;//	printf("随机生成矩阵A如下：\n");//	for (int i = 0; i < m; i++)//	{//		for (int j = 0; j < n; j++)//			//			matrixA[i][j] = rand() % (20 + 1);//			matrixA[i][j] = i + j;//	}//}//////////int main(void)//{//	srand((unsigned int)time(NULL));//	int choice;//	printf("1，随机生成三个3X3同阶整数方阵A.B.C验证公式:\n");//	printf("\t（1）	A(B+C)=AB+AC\n");//	printf("\t（2）	(AB)C=A(BC)\n");//	printf("\t（3）	(ABC)T=CT*BT*AT （T表示转置）\n");//	printf("2，生成单位矩阵，以 4 阶为例，并输出其转置矩阵\n");//	printf("3，退出\n");//	printf("请输入序号选择功能：");//	scanf("%d", &choice);//	while (choice)//	{//		printf("----------------------------------------------\n");//		switch (choice)//		{//		case 1:TestEquation();//			break;//		case 2:IMT();//			break;//		case 3:return 0;//		}//		printf("请输入序号选择功能：");//		scanf("%d", &choice);//	}//}